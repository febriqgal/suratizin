class Car {
  String color;
  String fuelType;
  int speed;
  int maxSpeed;

  Car(this.color, this.fuelType, this.speed, this.maxSpeed);

  void accelerate() {}
  void brake() {}
  void refuel(num liters) {}
}
